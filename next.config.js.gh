/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  assetPrefix: '/DeCryptography',
  basePath: '/DeCryptography',
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig
