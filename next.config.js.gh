/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  assetPrefix: '/DeCryptography/',
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig
